import styled from '@emotion/styled';
import { Card } from '@mui/material';

export const StyledCard = styled(Card)(() => ({
  boxShadow: 'none',
  borderRadius: '20px',
  height: '100%',
  padding: '8px',
}));
